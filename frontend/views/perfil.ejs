<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <link rel="stylesheet" href="/css/panel.css">
  <link rel="stylesheet" href="/css/infosUser.css">
  <link rel="shortcut icon" href="/imgs/box.png" type="image/x-icon">
  <title>Perfil</title>
</head>

<body>
  <div class="conteiner">
    <nav class="navegation">
      <%-include('includes/nav')%>
    </nav>

    <div class="panel">
      <header class="top">
        <div class="infos">
          <h1>Perfil do Usuário</h1>
        </div>
        <i class="bi bi-moon-fill theme"></i>
      </header>

      <main class="main">
        <article>
          <div class="infos-user">

            <div class="photo-user">
              <img src="/uploads/<%=user.photo%>" alt="user">
              <h3><%=user.name%></h3>
            </div>

            <div class="options">
              <button>Alterar senha</button>
              <button>Alterar nome</button>
              <button id="photo">Alterar imagem</button>
              <%if(user.admin) {%>
              <button>Ver perfils</button>
              <%}%>
            </div>

            <div class="info">
              <h4>Informação do usuário</h4>
              <p><span>Id:</span> <%=user._id%></p>
              <p><span>E-mail:</span> <%=user.email%></p>
              <p><span>Administrador:</span> <%= user.admin ? 'Sim' : 'Não' %></p>
            </div>
          </div>
        </article>
      </main>

      <dialog class="modal">
        <div class="headerModal">
          <p>Alterar imagem</p>
        </div>
        <div class="mainModal">
          <form method="POST" action="perfil/updatePhoto" enctype="multipart/form-data">
            <input type="file" name="file" id="file" accept="image/*">
            <input type="submit" value="Enviar">
          </form>
        </div>
        <div class="footerModal">
          <button class="closeButton">Fechar</button>
        </div>
      </dialog>
    </div>
  </div>
</body>

<script src="/js/modals.js"></script>
<script src="/js/theme.js"></script>

</html>